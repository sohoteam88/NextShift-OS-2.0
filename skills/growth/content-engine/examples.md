# Content Engine Examples

Use this skill when the user asks for work related to `content-engine`.

## Example Prompts

- Use `$content-engine` to help me design this part of NextShift.
- Use `$content-engine` to review my current idea and suggest the next step.
- Use `$content-engine` to generate an implementation-ready plan.

## Expected Output

Follow the output format in `SKILL.md`. Keep the result specific, practical, and ready for Claude Code or Codex to use.
