# OS 3.9 Pipeline Audit — W2/W3 landed: G4 hard-filter + G5 failure-visibility + G2 (lead-magnet/webinar) + O2 forked interview + O3 defaults·provenance · **all PRs CI GREEN incl. E2E**

| Field       | Value                                                                                          |
| ----------- | ---------------------------------------------------------------------------------------------- |
| Review Type | Code-level Pipeline Audit (mid-release; **not** a Release Audit)                                |
| Review Date | 2026-07-25                                                                                     |
| Reviewer    | Claude Code (Audit Engineer)                                                                   |
| Repository  | sohoteam88/NextShift-OS-2.0                                                                    |
| Branch      | `main` HEAD `f5ea865` (docs-only tip; `src` byte-identical to O3 feat `7403202` / merge `5c5e031`) |
| Blueprint   | `docs/nextshift-os-3/OS_3_9_BLUEPRINT.md` (APPROVED, Steven 2026-07-24)                        |
| Scope       | Items newly marked **已完成** since the 20260724-235932 W1 audit: **G4** (#151 / `f33c60d`), **G5** (#152 / `b5b1dc9`), **G2** (#155 / `8d208df`), **O2** (#163 / `1e1f2f9`), **O3** (#164 / `7403202`); plus re-confirmation that W1 (G0/G1/O1) is intact and prior condition **C-1** is resolved (#150 / `c1c1ef6`) |
| Verdict     | **PASS** (0 blockers; 0 conditions; 3 advisories)                                              |

---

## Executive Summary

Every claim below was verified against real source, real local runs on a clean tree at HEAD `f5ea865`, and authoritative GitHub Actions results. **No PR description was taken at face value** — each generation consumer's call path was traced end-to-end, the compliance regexes were hand-checked against the blueprint's four reject classes, the forked-interview state machine and default-fill were read line by line, the provenance store/merge path was traced through `getBrandDNA`/`saveBrandDNA`/`updateBrandDNA`, and the `src` subtree at HEAD was hash-matched to the CI-green O3 commit.

**All five items newly marked `已完成` (G4, G5, G2, O2, O3) are genuinely done, done at the layer the blueprint specified, and wired into product surface — not demos.** Combined with the previously-audited W1 (G0/G1/O1), OS 3.9 now has **eight** completed items and the picture is coherent:

- **G4 — output hard filter is a real, pure, reject-and-regenerate gate wired into every generation consumer.** `enforceComplianceHardFilter` (`hardFilter.ts`) rejects the four blueprint classes — `income_promise`, `medical_claim`, `weight_claim`, `public_price` — verb/number-anchored, and auto-rewrites brand/trademark residue to a safe generic term using the business-pack substitution table. It is consumed by **content-engine, lead-magnet, webinar-center, and forked-interview**, each running a bounded `reject → regenerate (≤2) → deterministic safe-base` loop so a rejected result is never persisted or shown.

- **G5 — failure is now visible, telemetry is best-effort, and a labelled retry reaches the UI.** `runGeneration` (`gateway.ts`) wraps `logAIUsage` in its own `try/catch` (a tracker failure can no longer discard a real, billed generation — closing prior advisory A-3), reports provider failure via `runtimeFallbackLogger.warn` which forwards to `Sentry.captureMessage` (closing prior advisory A-1), and returns the discriminated `template_fallback` outcome with `GENERATION_DEGRADE_LABEL`. `ContentGenerationDegradedNotice` renders the label + a "点此重试" button in the Content Command Center, and `ForkedInterviewExperience` surfaces "请点击重试" on degrade/reject (closing prior advisory A-2).

- **G2 — lead-magnet + webinar-center route through the same G0 seam and G4 gate.** Both `leadMagnetService.generate` and `webinarService.generate` build a public-safe business-pack context, call `runGeneration`, run the compliance loop, and set `generatedByAi`/`degradedLabel` honestly — structurally identical to G1's content-engine path.

- **O2 — forked interview is a data-driven 5-topic × 3-step funnel writing straight to versioned Brand DNA.** `generateFunnelConfirmation` composes a G0 context, calls `runGeneration` with an empty fallback that is *never shown or stored* (preserving G5's honest retry contract rather than faking AI), and `filterFunnelConfirmation` applies G4 **plus** a stricter B-path guard (opportunity stories cannot become income-promise copy). Confirmed sentences map directly into canonical DNA with **no secondary extraction**.

- **O3 — defaults fill every field, provenance is honest, and one version bump does it.** `fillForkedInterviewBrandDnaDefaults` completes the DNA from 5 confirmed answers + neutral, public-terminology coach defaults so `validateBrandDNA(...).missingFields` is empty (verified for **both A and B paths** in a co-located `it.each`), tags each field `user_confirmed` vs `coach_defaulted`, and persists provenance to a dedicated `User.metadata` key merged back on read. `confirmForkedInterviewTopic` fills once before a single `saveBrandDNA` (v→n+1, zero empty fields on first save, no double-save). A profile PATCH routes through `brandDnaService.updateBrandDNA`, flipping the edited field's provenance back to `user_confirmed` and advancing exactly one version. `BrandProfileStep` renders "教练猜的，点此改" **only** on `coach_defaulted` fields.

**The work was exercised by the pipeline, not merely asserted.** PRs **#151/#152/#155/#163/#164 each went green on all six checks including E2E Tests** (runs `30139410554` / `30140372908` / `30150024361` / `30152685408` / `30160083316`). I confirmed by tree hash that the `src` subtree at HEAD (`e318229d…`) is **byte-identical** to O3's feat commit `7403202` and its merge `5c5e031`; the only commit after the merge is the one-line blueprint update marking O3 complete. Local `type-check` (exit 0), `test` (**744 passed / 47 skipped**, 127 files), `build` (exit 0), `lint` (**0 errors**, 435 warnings), `lint:boundaries:check` ("in sync"), and `i18n:audit` (**1399 × en/ms/zh, 0 missing / 0 orphaned**) are all green. `prisma/`, `packages/`, and `.env*` have **zero diff** across the entire OS 3.9 release so far — no schema migration exists, so the blueprint's "含 schema 迁移 → 备份+隔离恢复演练" gate does not trigger this wave.

**Status bookkeeping is honest.** Only G0/G1/O1/G4/G5/G2/O2/O3 are marked done; **O4/O5/G3/G6/M1 are `未开始`, and that is true in the tree** — the old `/api/v1/video-production` pipeline (G3) still exists, no content-library dedup/cleanup script (G6) exists, and the M1 dual-track landing-page grep acceptance is not implemented. Nothing is over-claimed.

**Every open item from the prior W1 audit is now closed.** C-1 (`parseGeneratedPostJson` rejection branches untested) is resolved by #150's `contentPostGeneration.test.ts` (four `toThrow` cases + a fenced-JSON happy path). Prior advisories A-1 (Sentry on degrade), A-2 (render the degrade label), and A-3 (usage-logging failure masking real output) are all resolved by G5. There are no new blocking issues and no stated blueprint claim currently outruns its evidence, so this is a clean **PASS**. Three low-severity advisories are noted for the coming waves.

---

## CP1 — G4 output hard filter / compliance gate (blueprint: 已完成, PR #151 / `f33c60d`)

**Requirement:** post-generation enforcement (not prompt self-discipline): brand invisibility (rewrite on hit, table from the business pack), income-promise reject-and-regenerate (F-29), medical-claim ban, weight-number ban; batch assertions run three classes at zero hits; pipeline-side review gate (human Fable pass, no auto-merge).

**Every property is real and traced in source:**

- **Four reject classes, verb/number-anchored** — `findRejectedViolations` (`hardFilter.ts:51-99`) emits `income_promise` (月入/日入 + RM|千|万|k, and 保证收入/包赚/稳赚), `medical_claim` (`治` anchored with `政/自/法` lookbehind + `理` lookahead exclusions, 降三高, 减肥药, 保证瘦, cure/guaranteed weight loss), `weight_claim` (瘦/减重/减脂/掉 + number + 公斤/kg/斤), and `public_price` (`RM\s*[\d,]+`). A reject returns `{ status:'rejected', violations }` with **no `fields`** — the test asserts `not.toHaveProperty('fields')`. ✓
- **Brand invisibility by rewrite** — `rewriteText` (`hardFilter.ts:101-139`) applies substitution-table rules longest-first, then a catch-all `BARE_BRAND_PATTERN`/`PRODUCT_TRADEMARK_PATTERN` → `健康管理方案` for any unmapped `Herbalife/贺宝芙/康宝莱/Formula 1/NRG/Aloe`, each recorded as a visible `brand_hidden` / `brand_residual_rewritten` violation. ✓
- **Pure, no side-effects, business-pack via public barrel** — `enforceComplianceHardFilter` (`:146-167`) has no provider/DB/FS dependency and reads only `getComplianceRewriteRules({track})`. ✓
- **Wired into real generation with a bounded retry** — content-engine (`contentEngineService.ts:257-292`), lead-magnet (`leadMagnetService.ts:318-333`), webinar (`webinarService.ts:105-120`) all run `reject → runGeneration again (≤ MAX_COMPLIANCE_RETRIES=2) → filter the deterministic fallback → neutral safe base if even that rejects`. A rejected public result is **never** persisted. ✓
- **Tests cover all four classes + the false-positive guard** — `hardFilter.test.ts` drives income/medical/weight(+减重 variant)/price rejects, the single-character `治` claim, the `自治` non-medical false-positive, retail vs recruitment substitution scoping, and unlisted-brand residual rewrite. ✓
- **Pipeline review gate** — the "review 强制暂停" marker is a pipeline-side human gate, correctly not represented as blueprint state; PR #151's row notes two Fable review rounds before release. ✓

**Result: CP1 PASS.** (The blueprint's *live-model* "batch-generate N, three assertions zero-hit" acceptance is a release-level gate under `## 闸门与验收`, still unchecked — correct; it needs a live key and belongs to a release audit, not this mid-wave audit.)

---

## CP2 — G5 failure visibility (blueprint: 已完成, PR #152 / `b5b1dc9`)

**Requirement:** LLM failure → retry (router already) → on exhaustion **explicitly** mark failure + clickable retry, never silently fall back to template; batch tasks reuse F-24 single-item retry; provider errors reported; success-path telemetry hardened; UI renders the degrade notice + retry.

**Traced end-to-end:**

- **Telemetry is best-effort and cannot mask a real success** — `runGeneration` (`gateway.ts:36-47`) awaits `logAIUsage` inside its own `try/catch`; a tracker throw is swallowed and the genuine `source:'ai'` outcome is still returned. The gateway test mocks `logAIUsage` to reject and asserts the success outcome survives. ✓ (closes prior **A-3**)
- **Provider failure is reported, not silent** — the catch block (`gateway.ts:56-79`) calls `runtimeFallbackLogger.warn('[ai-generation] provider failed, degraded to template', …)`; `runtime-fallback-logger.ts` forwards to `Sentry.captureMessage`. The gateway test asserts `fallbackLogger.warn` is called exactly once with that message and `logAIUsage` is **not** called on failure. ✓ (closes prior **A-1**)
- **Explicit, discriminated degrade** — the catch returns `{ status:'degraded', source:'template_fallback', value: fallback, userVisibleLabel: GENERATION_DEGRADE_LABEL, reason }`; a consumer cannot read a template value as AI output. ✓
- **UI renders label + retry** — `ContentGenerationDegradedNotice` (`content-engine/components`) renders the label and a `点此重试` button (consumed by `ContentCommandCenter.tsx`); `ForkedInterviewExperience.tsx` sets "…请点击重试" on `degraded`, and "这句需要按合规要求重新生成，请点击重试" on `rejected`. ✓ (closes prior **A-2**)
- The hard-coded Chinese "点此重试" is acknowledged in the PR row as deferred to a later cross-module copy-migration batch — an accepted, tracked debt, and i18n:audit is still 0-missing across all three locales.

**Result: CP2 PASS.**

---

## CP3 — G2 lead-magnet + webinar-center through the gateway (blueprint: 已完成, PR #155 / `8d208df`)

**Requirement:** same G0 context; malformed webinar titles disappear at the root; dual-track content-direction forced-split (see M1).

- **lead-magnet** (`leadMagnetService.ts:290-336`): resolves `activeTrack`, builds a public-safe `getBusinessPackSlice({track,platform:'blog'})` context (+ its JSON instruction), calls `runGeneration` with `feature:'lead_magnet_generation'`, runs the G4 compliance loop, then sets `generatedByAi`/`degradedLabel` from the outcome and persists. ✓
- **webinar-center** (`webinarService.ts:81-124`): identical shape with `feature:'webinar_center_generation'`; the JSON-schema'd `parseGeneratedWebinar` contract replaces the old "audience stuffed into title" template, so the malformed-title class is gone at the source. ✓
- Both modules are among the four verified `runGeneration` + `getBusinessPackSlice` + `enforceComplianceHardFilter` consumers; neither instantiates a provider directly.
- **Note (process):** the blueprint row and commit `13b39f7` record G2 as "marked complete manually, pipeline aborted before Step 5.5." I checked the substance rather than the bookkeeping: **PR #155 itself went green on all six CI checks including E2E** (run `30150024361`), so the aborted step was pipeline automation, not a failed gate. The merged code is fully wired and green.

**Result: CP3 PASS.**

---

## CP4 — O2 forked interview (blueprint: 已完成, PR #163 / `1e1f2f9`)

**Requirement:** topic-1 fork (A product-first / B opportunity-first) → choice → fact follow-up → AI-composed confirmation; confirmation enters versioned Brand DNA with no secondary extraction.

- **5 topics × 3-step funnel with a real fork** — `getFunnelTopicSequence` yields `[entry_path, product_change, past_career, business_direction, weekly_rhythm]` for A and `[entry_path, change_reason, …]` for B (asserted in `funnelDefinition.test.ts`). Each topic runs option → (skippable) facts → confirmation. ✓
- **Confirmation via the G0 gateway, honest on failure** — `generateFunnelConfirmation` (`funnelConfirmationService.ts:48-85`) builds a track-scoped context, calls `runGeneration` (`feature:'brand_forked_interview_confirmation'`, temp 0.35) with an **empty fallback that is deliberately never shown or stored**; on `degraded` it returns the labelled degrade for the UI to retry rather than fabricating a sentence. ✓
- **G4 + stricter B-path guard** — `filterFunnelConfirmation` runs `enforceComplianceHardFilter`, then rejects any residual brand leak and, for `entryPath==='B'`, any income/赚钱/回本/财务自由 wording. ✓
- **No secondary extraction** — `confirmForkedInterviewTopic` (`brand-interview-service.ts:367-395`) maps confirmed sentences straight into DNA via `mapConfirmedFunnelToBrandDna` and saves through versioning; the funnel test asserts `extractBrandProfile` is **never** called. ✓

**Result: CP4 PASS.**

---

## CP5 — O3 defaults fill + confidence/provenance (blueprint: 已完成, PR #164 / `7403202`)

**Requirement:** business-pack defaults + 5-answer inference → zero empty fields; low-confidence fields marked "教练猜的，点此改" in-product; corrections write back Brand DNA version +1.

- **Zero empty fields, both paths** — `fillForkedInterviewBrandDnaDefaults` (`brandDnaDefaults.ts:78-152`) fills every non-confirmed field with neutral, public-terminology defaults (`content.contentPillars` padded to ≥3, `visual.brandColors` to ≥2), while the 7 `FUNNEL_CONFIRMED_FIELDS` come from the confirmed mapping. `funnelDefinition.test.ts` proves `validateBrandDNA(completed).missingFields === []` for **both A and B** via `it.each`, and cross-checks the completed DNA against every `findMissingFields` rule (`brandDnaValidator.ts:77-103`). ✓
- **Honest provenance, dedicated store, merged on read** — provenance is keyed `${section}.${field}` (`user_confirmed` for confirmed fields; `coach_defaulted` where a base default actually changed the value, computed by JSON-compare). It persists to the dedicated `brand_dna_field_provenance` `User.metadata` key (`brandDnaService.ts:49-60,118-127`) and is merged back in `getBrandDNA` via `withFieldProvenance` — so it survives the `BrandProfile`-table reload with **no schema migration**. The test asserts the exact `user_confirmed`/`coach_defaulted` map. ✓
- **One fill, one save, v→n+1, no double-save** — `confirmForkedInterviewTopic` fills once, then a single `saveBrandDNA` (the sole version incrementer, +1); the funnel test asserts `result.dna.meta.version === 2` from an empty base. ✓
- **Profile PATCH edits flip provenance and bump exactly one version** — the profile route calls `brandDnaService.updateBrandDNA`, which sets each patched field's provenance to `user_confirmed` and delegates the +1 to `saveBrandDNA` (`brandDnaService.ts:132-146`, with an explanatory comment). Non-DNA legacy metadata keys are preserved by the `...existingMeta` spread. ✓
- **UI marker only on coach defaults** — `BrandProfileStep.tsx:274,290,243` renders "教练猜的，点此改" only when `provenance?.[field.provenanceKey] === 'coach_defaulted'`. ✓
- **Neutral copy is compliant** — the test asserts the completed DNA JSON matches no brand name / `RM<digits>` / weight number / income-guarantee. ✓

**Result: CP5 PASS.**

---

## CP6 — Repo & CI integrity — **GREEN incl. E2E**

**Authoritative CI evidence (GitHub Actions), all six checks green each:**

| PR | Head | Result |
| --- | --- | --- |
| **#151** (G4) run `30139410554` | `f33c60d`-chain | ✅ Type Check+Lint+Build · Tests · E2E Secret Check · **E2E Tests (12m3s)** · Migration Image Build · Healthcheck |
| **#152** (G5) run `30140372908` | `b5b1dc9`-chain | ✅ all 6, incl. **E2E Tests (12m42s)** |
| **#155** (G2) run `30150024361` | `8d208df`-chain | ✅ all 6, incl. **E2E Tests (11m13s)** |
| **#163** (O2) run `30152685408` | `1e1f2f9`-chain | ✅ all 6, incl. **E2E Tests (11m26s)** |
| **#164** (O3) run `30160083316` | `7403202`-chain | ✅ all 6, incl. **E2E Tests (12m31s)** |

**Does green CI cover the code at HEAD?** Verified by tree hash:

```
src subtree @ 7403202 (O3 feat, green incl. E2E) = e318229decd0eb93ac8e88fc7579d03a5e912686
src subtree @ 5c5e031 (merge #164)               = e318229decd0eb93ac8e88fc7579d03a5e912686
src subtree @ HEAD    (f5ea865)                   = e318229decd0eb93ac8e88fc7579d03a5e912686
git log 5c5e031..HEAD → docs(blueprint): mark O3 completed (1 file, 1 line)
```

**Byte-identical.** Every source line on `main` was executed by a fully green CI run including E2E; HEAD (`f5ea865`, docs-only) has no run of its own by the standing `paths-ignore` on `docs/**`.

**No new storage across the whole release:** `git diff --stat fef84f2^..HEAD -- prisma/ packages/ .env*` is **empty**. O1 (JSON asset) and O3 (provenance in `User.metadata`) both avoid schema changes, so the "含 schema 迁移 → 备份+隔离恢复演练" gate does **not** trigger.

**Result: CP6 PASS.**

---

## CP7 — Full verification suite

Every row executed locally for this audit at HEAD `f5ea865` on a clean tree. Nothing carried from a PR description.

| Check | Result | Evidence |
| --- | --- | --- |
| `type-check` | ✅ PASS | `tsc --noEmit`, exit 0 |
| `lint` | ✅ PASS | `eslint .` → **0 errors**, 435 warnings (pre-existing `no-restricted-imports` cross-module advisories + the one sanctioned `@/modules/brand-dna` type import in `brandDnaDefaults.ts` carrying an inline `eslint-disable`) |
| `lint:boundaries:check` | ✅ PASS | **"eslint-boundaries.config.mjs is in sync"** |
| `test` | ✅ PASS | `vitest run` → **744 passed / 47 skipped** (791), 127 files passed / 8 skipped (~61s); +64 tests vs the W1 audit's 680 |
| `build` | ✅ PASS | `next build`, exit 0 |
| `i18n:audit` | ✅ PASS | **1399** keys × en/ms/zh, **0 missing / 0 orphaned** |
| **CI @ #151/#152/#155/#163/#164** | ✅ **PASS** | all six checks green each, incl. **E2E Tests** |
| E2E | ✅ **RAN GREEN** | on a **byte-identical `src` tree** (subtree hash `e318229d…` verified) |
| `prisma`/`packages`/`.env` diff (whole release) | ✅ **ZERO** | no schema migration |
| direct provider instantiation (audited modules) | ✅ **ZERO** | architecture rule upheld (grep `new OpenAI|new Anthropic|GoogleGenerativeAI`) |

**Result: CP7 PASS.**

---

## CP8 — Blueprint ↔ code consistency & scope honesty

- **The eight `已完成` rows match the code.** G0/G1/O1 re-confirmed intact (W1 audit + C-1 now closed); G4/G5/G2/O2/O3 verified above against source, tests, and CI. ✓
- **The `未开始` rows are honestly unstarted:** the old `/api/v1/video-production` template pipeline still exists (**G3** not retired); no content-library dedup/cleanup script exists (**G6**); Review Room / just-in-time field trigger (**O4**) and the F-14 hard-gate removal (**O5**) are not implemented; and **M1**'s dual-track landing-page grep acceptance is not implemented. Nothing is over-claimed. ✓
- **The `## 闸门与验收` release-gate checklist is still fully unchecked**, which is correct — aunt test, 3× live-model generation-truthfulness, live-model compliance zero-hit batch, dual-track grep, and provider-down drill are release-level acceptance items requiring a live key, owned by a later release audit, not this mid-wave pipeline audit. ✓
- **F-33 (M1) note:** the blueprint records M1's two-layer root cause (Brand-DNA target field storing both tracks concatenated; the funnel "AI 文案" path bypassing G0 mode-isolation + G4). The completed items already mitigate this partially — `mode` is a first-class context field in every `runGeneration` call, G4 filters compliance classes on all output, and the O2 funnel now writes single-track `targetAudience` — but the full track-scoped split + landing-page grep acceptance are correctly deferred to M1 (未开始). See advisory A-2.

**Result: CP8 PASS.**

---

## Findings

| ID | Severity | Checkpoint | Finding |
| --- | --- | --- | --- |
| A-1 | Advisory (effectiveness wrinkle, low) | CP1/CP2 | **The compliance regeneration loop re-runs an identical prompt with no violation feedback.** On a G4 `rejected`, content-engine/lead-magnet/webinar/forked-interview call `runGeneration` again with the *same* `userMessage` and context; the only source of variation is `temperature`. A model that reliably emits a violation (e.g., a persistent public-price mention) will re-hit it through both retries and then land on the neutral **safe-base** result labelled `GENERATION_DEGRADE_LABEL` ("AI 暂时不可用，这是基础版本") — mislabelling an *available-but-non-compliant* AI as "AI unavailable." Bounded and always safe (never persists a rejected result), so non-blocking; consider appending the caught violation codes to the retry prompt so regeneration is corrective rather than a re-roll. |
| A-2 | Advisory (M1 scope — pending) | CP8 | **F-33 dual-track leakage is only partially mitigated in completed items; the full fix is M1 (未开始).** `mode` is threaded as first-class generation context and G4 catches compliance-class leaks, but G4 does not catch *topical* cross-track bleed (a retail post drifting into recruitment "系统带你做" framing), and legacy Brand DNA written before the O2 funnel may still concatenate both tracks in one target field. This is exactly M1's remit and is honestly marked unstarted — flagged so the acceptance grep (零售页无 创业/副业/收入 词根; 招募页无疗效词根) is run against the F-33 screenshot scenario when M1 lands. |
| A-3 | Advisory (copy-migration debt, tracked) | CP2 | **The G5 retry copy ("点此重试" / "请点击重试") is hard-coded Chinese**, acknowledged in the PR row as deferred to a later cross-module i18n batch. `i18n:audit` remains 0-missing across en/ms/zh because these strings are not yet keyed; noted so the debt is closed when the copy-migration batch runs. |

**No correctness, security, or tenant-isolation defect was found in anything marked done.** Tenant scoping flows correctly through every consumer (`enforceQuota(tenantId)` → `getRouterForTenant(tenantId)` → `logAIUsage({tenantId})` → `Content.tenantId`/`BrandProfile.tenantId` persisted from the caller). The compliance gate is a pure, dependency-free function proven against all four reject classes and the `治`/`自治` false-positive. The generation degrade contract is a discriminated union that structurally prevents reading a template value as AI output. Brand-DNA provenance is stored in a dedicated metadata key and merged on read, surviving the canonical-table reload with no migration.

---

## Conditions to clear

**None.** Every open item from the prior W1 audit is resolved:

- **C-1** (prior) — `parseGeneratedPostJson` rejection branches now have direct negative-path unit tests (#150 / `c1c1ef6`): non-JSON, empty required field, `body` too short, and non-string-array `hashtags` all assert `toThrow`, plus a fenced-JSON happy path. **Resolved.**
- Prior **A-1** (Sentry on degrade) — resolved by G5's `runtimeFallbackLogger` → `Sentry.captureMessage`.
- Prior **A-2** (render the degrade label) — resolved by `ContentGenerationDegradedNotice` + `ForkedInterviewExperience`.
- Prior **A-3** (usage-logging failure masking real output) — resolved by G5's best-effort `logAIUsage` try/catch.

The three advisories above are recommendations for the coming waves (A-1 folds into G4 retry-quality tuning; A-2 is M1's remit; A-3 is a tracked i18n batch), not gates.

**Process note — discipline held.** All five PRs merged green across all six checks including E2E; the boundaries config stayed in sync; `prisma`/`env`/`packages` are untouched for the entire release; the architecture rule (router-only LLM access) is upheld across all four generation consumers; and the blueprint status table matches the tree exactly, with the unstarted W3 tail (O4/O5/G3/G6/M1) honestly unstarted.

---

## Verdict

**PASS.**

The OS 3.9 compliance-and-onboarding waves are real and honest. **G4** is a pure, verb/number-anchored public-output gate that rejects the four blueprint classes and rewrites brand residue, wired into a bounded reject→regenerate→safe-base loop in all four generation consumers so a non-compliant result is never persisted or shown. **G5** makes failure visible: telemetry is best-effort (a tracker throw can no longer discard a real generation), provider failure is reported to Sentry via `runtimeFallbackLogger`, and the labelled degrade + "点此重试" reaches the UI — closing all three of the prior audit's advisories. **G2** routes lead-magnet and webinar-center through the identical G0 seam and G4 gate. **O2** is a data-driven 5-topic × 3-step forked funnel that writes confirmed sentences straight into versioned Brand DNA with no secondary extraction and honours G5's retry contract on failure. **O3** fills every field to `missingFields: []` (verified for both A and B paths), tags each field's provenance, persists it to a dedicated metadata key merged on read with no schema migration, bumps exactly one version per fill or edit, and surfaces "教练猜的，点此改" only on coach-defaulted fields.

The work was exercised, not asserted: PRs #151/#152/#155/#163/#164 each went green on all six CI checks including E2E, and the `src` subtree at HEAD is byte-identical to the CI-green O3 commit, one docs line apart. Local type-check, lint (0 errors), 744 tests, build, boundaries ("in sync"), and i18n (1399 × 3, zero missing) are all green, with zero `prisma`/`env`/`packages` diff across the entire release. Tenant scoping is correct, the architecture rule holds, the prior condition C-1 is closed, and the `未开始` W3 tail (O4/O5/G3/G6/M1) is honestly unstarted.

Ship-blocking issues: **none.** The three advisories (identical-prompt regeneration re-roll; F-33 topical cross-track bleed owned by M1; hard-coded retry copy owned by a later i18n batch) are all low-severity, bounded, and belong to the coming waves. This audit clears the OS 3.9 W2/W3-partial checkpoint; the live-model acceptance gates under `## 闸门与验收` remain, correctly, for a later release audit.
