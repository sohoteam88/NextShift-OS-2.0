# CRM Revenue Command Center Audit — 2026-06-12

Existing: Lead, Customer, Activity, PipelineStage, Tag, LeadTag Prisma models. API routes under /api/v1/crm/. Reuse all existing models. New module adds revenue forecast, hot lead engine, CRM advisor, opportunity tracking overlay. Stores config in user.metadata.crm_command.
