# Retention System Examples

Use this skill when the user asks for work related to `retention-system`.

## Example Prompts

- Use `$retention-system` to design this CRM workflow for NextShift.
- Use `$retention-system` to turn my current CRM process into a clear system.
- Use `$retention-system` to generate implementation-ready CRM specs.

## Expected Output

Follow the output format in `SKILL.md`. Keep the result practical, CRM-specific, and ready for Codex or Claude Code to use.
