# CRM Database Examples

Use this skill when the user asks for work related to `crm-database`.

## Example Prompts

- Use `$crm-database` to design this NextShift data layer.
- Use `$crm-database` to create implementation-ready specs for Supabase/Postgres or analytics.
- Use `$crm-database` to review whether my current data model can support reporting.

## Expected Output

Follow the output format in `SKILL.md`. Keep the result data-specific, implementation-ready, and clear enough for Codex or Claude Code to use.
